## Get underlying data 
### stocks.fa.score(symbol: str) -> Optional[numpy.number]

Gets value score from fmp

    Parameters
    ----------
    symbol : str
        Stock ticker symbol

    Returns
    -------
    np.number
        Value score
