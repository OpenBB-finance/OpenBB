```
usage: normality [-h] [--export {csv,json,xlsx}]
```

Normality tests

```
optional arguments:
  -h, --help            show this help message (default: False)
  --export {csv,json,xlsx}
                        Export raw data into csv, json, xlsx (default: )
```
