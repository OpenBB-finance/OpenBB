```
usage: calendar [-d DAYS] [-h]
```

Show Calendar Data

```
optional arguments:
  -d DAYS, --days DAYS  The number of days to search for, up to 30 forward or backward use negative numbers to search back.
  -h, --help            show this help message
```
