name: 🧪 test openbb-tmx

on:
  pull_request:
    branches: [develop, main, v5, feature/v5-platform-api]
    paths:
      - "openbb_platform/providers/tmx/**"
      - ".github/workflows/test-unit-openbb-tmx.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: test-openbb-tmx-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build sdist + wheel
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: openbb_platform/providers/tmx
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - run: python -m pip install --upgrade pip uv

      - name: Build sdist + wheel
        run: uv build --no-sources

      - uses: actions/upload-artifact@v4
        with:
          name: openbb-tmx-dist
          path: openbb_platform/providers/tmx/dist/
          if-no-files-found: error
          retention-days: 7

  lint:
    name: Lint (ruff + ty)
    needs: build
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
        working-directory: openbb_platform/providers/tmx
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - run: python -m pip install --upgrade pip uv

      - name: Install openbb-tmx in editable mode (+ dev group)
        run: |
          uv pip install --system -e '.[all]'
          uv pip install --system --group dev

      - name: Verify the optional extras resolved
        run: python -c 'import openbb_charting, pywry'

      - name: ruff check
        run: ruff check openbb_tmx tests integration

      - name: ruff format --check
        run: ruff format --check openbb_tmx tests integration

      - name: ty check
        run: ty check openbb_tmx

  test:
    name: Test (${{ matrix.os }} / Python ${{ matrix.python_version }})
    needs: build
    strategy:
      fail-fast: false
      matrix:
        include:
          - { os: ubuntu-latest, python_version: "3.10" }
          - { os: ubuntu-latest, python_version: "3.11" }
          - { os: ubuntu-latest, python_version: "3.12" }
          - { os: ubuntu-latest, python_version: "3.13" }
          - { os: ubuntu-latest, python_version: "3.14" }
          - { os: macos-latest, python_version: "3.10" }
          - { os: macos-latest, python_version: "3.14" }
          - { os: windows-latest, python_version: "3.10" }
          - { os: windows-latest, python_version: "3.14" }
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        shell: bash
        working-directory: openbb_platform/providers/tmx
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python_version }}
          allow-prereleases: true

      - run: python -m pip install --upgrade pip uv

      - name: Install openbb-tmx from source (+ dev group)
        run: |
          uv pip install --system -e '.[all]'
          uv pip install --system --group dev

      - name: Verify the optional extras resolved
        run: python -c 'import openbb_charting, pywry'

      - name: Run unit tests
        run: pytest tests --cov=openbb_tmx --cov-report=term-missing --cov-fail-under=100
