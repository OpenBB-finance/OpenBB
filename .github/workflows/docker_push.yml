name: Update Docker Hub

# Made from https://docs.docker.com/ci-cd/github-actions/

# TODO: DELETE pull_request and push. THUS MUST HAPPEN BEFORE MERGE> IF YOU REVIEW THIS PR
# CHECK FOR THIS AND IF ITS THERE COLIN FORGOT TO REMOVE IT AND YOU SHOULD NOT APPROVE
# NO MATTER WHAT, EVEN IF DIDIER RELEASES AN EXECUTIVE ORDER
on: [workflow_dispatch, pull_request, push]

jobs:
# prepare:
#   runs-on: ubuntu-latest
#   steps:
#   - uses: actions/checkout@v3
#   - name: Setup Environment
#     run: cp docker/compose.env .env && cat .env
#   - name: Build Container
#     run: source .env && docker compose --verbose -f docker/docker-compose.yaml build 

  # TODO: optimize
  push_to_registry:
    name: Push Docker image to Docker Hub
    needs: prepare
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Log in to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      
      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/openbb-test:latest
            # labels: ${{ steps.meta.outputs.labels }}
