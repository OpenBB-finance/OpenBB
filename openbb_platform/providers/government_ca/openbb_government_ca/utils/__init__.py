"""Utility package for ``openbb-government-ca``.

Sub-packages
------------
- ``metadata``  — thread-safe singleton + cache mixin (LZMA-compressed JSON)
- ``generate_cache`` — build-time CLI script (stdlib + ``requests`` only)
- ``helpers`` / ``constants`` — shared runtime helpers
"""
