(function(){var SEL='#analysis_dialog,#download_dialog,#visualization_dialog,#filter_dialog';function clamp(){var box=document.getElementById('simplemodal-container');if(!box)return;var el=box.querySelector(SEL)||box.firstElementChild;if(!el)return;var pad=6,r=el.getBoundingClientRect(),w=document.documentElement.clientWidth,cur=parseFloat(getComputedStyle(el).left)||0,dx=0;if(r.left<pad)dx=pad-r.left;else if(r.right>w-pad)dx=Math.max(w-pad-r.right,pad-r.left);if(dx)el.style.left=(cur+dx)+'px';}new MutationObserver(clamp).observe(document.documentElement,{childList:true,subtree:true});window.addEventListener('resize',clamp);})();